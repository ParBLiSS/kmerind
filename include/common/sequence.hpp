// sequence is a contiguous collection of words.
// it represents a read, a chromosome, a contig, etc.

// the idea is for this to represent a physically or conceptually contiguous sequence.

// depending on the expected length of the sequence, the choosen data structure may be
// different:  std::array, std::vector, etc.
